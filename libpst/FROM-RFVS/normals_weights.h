#include <r3.h>

double computeWalb(double albedo, double Amax);

double computeWnrm(r3_t norm, r3_t luz_dir, double r);

double computeWsmp(double S[], int nLights);

double computeWhigh(r3_t norm,r3_t cluster_dir,double k,double r);

